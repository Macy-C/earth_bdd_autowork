# Recorder Adjust (Retired)

This prompt is retained only for compatibility with historical links.

Do not use it to inspect Requests, answer Decisions, submit Designs, or modify
BDD files. Current Recorder generation starts from a Workbench-created
Generation Job and uses `/recorder-generate <job-path>`.

For a failed, stale, or retired Job, return to the Workbench. Create a retry
Job only through the current Job lifecycle; use the Job-bound inspection and
repair flow exposed by that Job.