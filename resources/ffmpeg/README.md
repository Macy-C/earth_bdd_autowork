# FFmpeg

Place the bundled Windows FFmpeg executable here:

```text
resources/ffmpeg/current/bin/ffmpeg.exe
```

The recorder uses this project-local binary by default. `RECORD_SETTING.FFMPEG_PATH` can point to another executable when needed.
